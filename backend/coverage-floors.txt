# Per-file coverage floors for the ClinicOS fork delta.
#
# ONLY files this fork owns belong here. Gating upstream files would mean an upstream rebase
# reddens our CI for code we did not write.
#
# Floors are seeded at each file's measured coverage and may only ever be RAISED. Enforced by
# scripts/check-coverage.sh in .github/workflows/go.yml.
#
# Target for the Access & Setup domain (archon#1667): 95% lines on every file below. Files still
# at 0 are covered by later streams of #1667 (device trust = S1 tasks 7-9); their floor rises when
# that work lands. A floor stays below 95 only with a written reason, per #1667 §3.4 assumption A2.
#
# <path relative to backend/>                                        <minimum % statements>
#
# SCOPE: this PR is a TEST HARNESS only -- it adds unit tests + this gate against the CURRENT
# (unmodified) code. It ships NO production change. The archon#1668 tenant-boundary fix and the
# #1667 device-trust eviction/OQ3 hardening live on their own branch (test/hanko-device-trust-s1-refactor)
# and land as a separate refactor+fix PR, so that the safety net is in place first (#1667 §3.4).

# --- Pure, unit-testable functions this harness covers ---

# 93.8% ceiling: the `!ok || !token.Valid` branch is unreachable -- err != nil already returned,
# and the claims type is the pointer we passed to ParseWithClaims. Exempted from 95 per A2.
flow_api/flow/credential_usage/service_token.go                      93

# 96.2% -- covers the cookie codec (Parse/Serialize round-trip), CheckDeviceTrust, CreateTrustedDevice
# and GenerateRandomToken. The two uncovered lines are the crypto/rand failure branches, not
# deterministically reachable in a unit test (A2 defensive exemption).
flow_api/services/device_trust.go                                    95

# 100% -- pure tenant-boundary decision logic (validateTenantBoundary, shouldAdoptGlobalUser,
# resolveServiceTokenUser). Extracted from the flow action so the #1668 cross-tenant check is
# assertable without dockertest + a live Postgres.
flow_api/flow/credential_usage/tenant_boundary.go                    95

# --- Flowpilot orchestration: driven through the fake-context harness (flowpilot/mock_context.go, archon#1874 P1) ---

# 94.7% -- Execute driven through a fake flowpilot ExecutionContext (accept / #1668 cross-tenant
# reject / invalid-token / rate-limit / global-user-adopt paths) plus GetName/GetDescription/
# Initialize. The two uncovered lines are the defensive `err != nil` returns after Limit2 and
# Payload().Set -- neither errors with an in-memory limiter store / sjson on a valid path (A2
# defensive exemption). Floored at the honest achieved value < 95.
flow_api/flow/credential_usage/action_preauthenticated_continue.go   94

# 97.1% -- Execute driven through a fake flowpilot HookExecutionContext (OQ3 non-positive-duration
# no-write guard, active-write happy path, policy never/prompt short-circuits, missing/malformed
# user id, persist failure, existing-cookie merge). The one uncovered line is the crypto/rand
# failure branch in GenerateRandomToken, not deterministically reachable in a unit test (A2).
flow_api/flow/device_trust/hook_issue_trust_device_cookie.go         95

# --- Adjacent auth-flow files (archon#1874 P6) ---

# 100% -- the multi-tenant resolution middleware that populates deps.TenantID for the #1668 path:
# header parse, existing/disabled tenant, auto-provision on/off + create failure, global-user gate,
# and the GetTenantID/GetTenant accessors, all driven through a faked TenantPersister.
middleware/tenant.go                                                 95

# 90.6% -- Limit / Limit2 (allow + deny + stopped-store error) and every CreateRateLimit*Key builder,
# plus NewRateLimiter's in-memory and redis constructors. The 3 uncovered statements are the redis
# Dial closure (only runs on a live dial, never in a unit test) and the two log.Fatal branches after
# memorystore/redisstore.New, which only fire on a nil config -- not deterministically reachable
# (A2 defensive exemption). Floored at the honest achieved value < 95.
rate_limiter/rate_limiter.go                                         90

# 95.8% -- NewManager SameSite mapping (lax/strict/none/default) + audience, GenerateJWT (template,
# username, tenant_id claim, WithValue option, sign failure), GenerateCookie, Verify, DeleteCookie.
# The two uncovered lines are the crypto/rand session-id failure and the ProcessJWTTemplate error
# return (that function never returns non-nil) -- neither reachable in a unit test (A2 defensive).
session/session.go                                                   95

# 96.8% -- PasswordLogin.Execute driven through the fake flowpilot ExecutionContext: invalid input,
# rate-limit allow/deny/error, email + username lookup (found / not-found / global-adopt / adopt-error
# / lookup-error), wrong-password audit + audit-error, verify-error, hook-error, happy path. The two
# uncovered lines are the defensive `err != nil` returns after Stash().Set / Payload().Set, which do
# not fail on an in-memory sjson path (A2 defensive exemption).
flow_api/flow/credential_usage/action_password_login.go              95

# 93.3% -- ContinueWithLoginIdentifier.Initialize + Execute through the fake context: identifier /
# email-only / username-only analysis, SAML redirect + auth-url error, email & username resolution
# (found / unknown+hints / global-adopt / adopt-error / lookup-error / get-error / audit-error),
# both privacy modes incl. the OnlyShowActualLoginMethods chooser / passcode / password / passkey
# branches, and the discontinuity errors. The 11 uncovered statements are all defensive `err != nil`
# returns after Stash().Set / Payload().Set, none of which fail on an in-memory sjson path (A2
# defensive exemption). Floored at the honest achieved value < 95.
flow_api/flow/credential_usage/action_continue_with_login_identifier.go 93
